iterations = 1
# The variable iterations holds the value we are using for counting, the condition (iterations < 6) tells Ruby when to stop this loop (when the value of iterations is greater or equal to 6), and the iterations += 1 advances the counter to make progress.

while iterations < 6 do
   puts iterations
   iterations += 1
end